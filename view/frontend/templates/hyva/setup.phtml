<div class="container mx-auto px-4 py-8">
    <?php if ($block->is2faConfiguredForCustomer()): ?>
    <div class="w-full bg-white shadow-md rounded-lg p-6">
        <div class="text-gray-700">
            <p><?php echo $block->escapeHtml(__('Two-Factor Authentication already configured. Contact support if you wish to reset it.')); ?></p>
        </div>
    </div>
    <?php else: ?>
    <div class="flex flex-col md:flex-row gap-6 items-start">
        <div class="w-full md:w-1/2">
            <img src="<?php echo $block->escapeUrl($block->getQrCodeBase64Image()); ?>" 
                 alt="2FA Authenticator" 
                 class="w-full max-w-xs mx-auto shadow-lg rounded-lg" />
        </div>
        <div class="w-full md:w-1/2">
            <p class="mb-6 text-gray-700"><?php echo $block->escapeHtml(__('Download a 2FA app on your phone to scan the QR code and get the authentication code, for example Google Authenticator or Authy.')); ?></p>
            <div class="bg-white shadow-md rounded-lg p-6">
                <form class="space-y-6" 
                      action="<?php echo $block->escapeUrl($block->getSetupFormAction()); ?>" 
                      method="post" 
                      data-hasrequired="<?php echo $block->escapeHtmlAttr(__('* Required Fields')); ?>">
                    <input type="hidden" name="secret" value="<?php echo $block->escapeHtmlAttr($block->getSecretCode()); ?>" />
                    <fieldset>
                        <div class="space-y-2">
                            <label class="block text-sm font-medium text-gray-700" for="code">
                                <span><?php echo $block->escapeHtml(__('Authentication Code')); ?></span>
                            </label>
                            <div>
                                <input name="code" 
                                       id="code" 
                                       title="<?php echo $block->escapeHtmlAttr(__('Authentication Code')); ?>" 
                                       value="" 
                                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                       type="text" 
                                       autocomplete="one-time-code"
                                       data-validate="{required:true}"/>
                            </div>
                        </div>
                    </fieldset>
                    <div class="flex justify-start">
                        <button type="submit" 
                                title="<?php echo $block->escapeHtmlAttr(__('Authenticate')); ?>" 
                                class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-md transition-colors duration-200">
                            <span><?php echo $block->escapeHtml(__('Authenticate')); ?></span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <?php endif; ?>
</div>
